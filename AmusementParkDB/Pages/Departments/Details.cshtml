@page
@model AmusementParkDB.Pages.Departments.DetailsModel

<h4>Department</h4>
<hr />

<div>
    <dl class="row">
        <dt class="col-sm-2">Department Name</dt>
        <dd class="col-sm-10">@(Model.Department.Name ?? "-")</dd>

        <dt class="col-sm-2">Description</dt>
        <dd class="col-sm-10">@(Model.Department.Description ?? "-")</dd>
    </dl>
</div>

<div>
    <a asp-page="./Edit" asp-route-id="@Model.Department.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
